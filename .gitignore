# -------- Flutter / Dart (standard) --------
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
.flutter-versions
.melos_tool/
**/build/
**/.generated/
.metadata

# -------- IDE / OS --------
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db
Desktop.ini

# -------- Android --------
android/.gradle/
.gradle/
android/local.properties
android/key.properties
android/app/*.keystore
android/app/*.jks
# If you want to keep Firebase files private on GitHub, ignore them:
android/app/google-services.json
android/app/src/**/google-services.json
android/app/src/**/crashlytics*.properties
android/app/src/**/fabric.properties

# -------- iOS / macOS --------
ios/Pods/
macos/Pods/
# Xcode user data & DerivedData
**/DerivedData/
**/*.xcuserdatad/
**/*.xcworkspace/xcuserdata/
# Keep Firebase files private if desired:
ios/Runner/GoogleService-Info*.plist

# -------- Firebase / Functions / Secrets --------
# FlutterFire options file (contains API keys – not secret by design, but ignore if you prefer to keep private)
lib/firebase_options.dart
# Admin service accounts & runtime configs
**/serviceAccount*.json
**/*firebase-adminsdk*.json
**/.runtimeconfig.json
# Dotenv files
.env
.env.*
*.env
*.env.local

# -------- Keys & Certificates --------
**/*.keystore
**/*.jks
**/*.p12
**/*.p8
**/*.pem
**/*.key
**/*.crt
**/*.der
**/*.asc

# -------- Logs / Reports --------
*.log
logs/
firebase-debug.log
coverage/

# -------- Web / Messaging --------
web/.firebase/
web/firebase-messaging-sw.js

# -------- Desktop (Linux/Windows/macOS build junk) --------
linux/flutter/ephemeral/
windows/**/x64/
windows/**/x86/

# -------- App-specific accidental tokens (from your ZIP) --------
accessToken_token.code-search
*accessToken*.*
*token*.*
*.code-search

# -------- Keep tracked (DO NOT ignore) --------
# pubspec.yaml and pubspec.lock should be tracked for apps to ensure reproducible builds.
# /assets/ (except if it contains secrets, then create a separate /assets_private/ and ignore that)
lib.zip
