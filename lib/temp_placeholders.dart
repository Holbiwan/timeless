import 'package:flutter/material.dart';

class JobRecommendationScreen extends StatelessWidget {
  const JobRecommendationScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('JobRecommendationScreen (placeholder)')),
      );
}

class ManagerDashboardScreen extends StatelessWidget {
  const ManagerDashboardScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('ManagerDashboardScreen (placeholder)')),
      );
}

class NewHomePageScreen extends StatelessWidget {
  const NewHomePageScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('NewHomePageScreen (placeholder)')),
      );
}
